# CoursePilot 仓库协作说明

本文件适用于整个 CoursePilot 仓库。

## 开始工作前

1. 先完整阅读 `spec.md`，它是产品需求、接口、数据模型、评测门槛和里程碑的权威来源。
2. 再阅读 `PROJECT_MEMORY.md`，了解项目由来、已锁定决策、与 Coderook 的边界及秋招作品集目标。
3. 若最新用户指令与文档冲突，以最新用户指令为准，并同步更新相应文档。

## 不可破坏的产品约束

- 未经教师审核的知识节点、关系和题目不得进入学生正式问答、掌握度或学习路径。
- 证据不足时明确拒答，不使用模型常识伪装课程资料答案。
- 每次数据访问都在服务端校验用户、角色、课程和资源归属，禁止跨课程或跨用户泄漏。
- 只有教师审核题目的有效作答可以更新掌握度；更新必须幂等、事务化并可审计。
- 新课程版本未完成全部索引前继续使用旧版本；发布切换必须可追溯。
- 不在 CoursePilot 中实现 Coderook 已覆盖的代码执行和代码修改能力。
- 不预填或虚构简历指标，所有数字必须能够追溯到冻结评测报告或可复现实验。

## 实现习惯

- 按 `spec.md` 第 14 节的周里程碑推进，不要一次性搭建与 MVP 无关的大型基础设施。
- 后端新增接口时同时补充权限测试、错误契约和必要的集成测试。
- 检索或 Agent 行为变更时同步更新冻结评测或回归用例，避免只依赖人工演示。
- 影响产品范围、核心架构、数据契约或验收指标的决定，追加到 `PROJECT_MEMORY.md` 的决策记录。
- 引入第三方实现前检查许可证；需要时更新 NOTICE 和依赖清单。

## 文档优先级

```text
最新用户指令
  → spec.md
  → PROJECT_MEMORY.md
  → README 与其他实现文档
  → 代码注释
```
